version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No build step required"
    build:
      commands:
        - echo "Static site build complete"
  artifacts:
    baseDirectory: AjibolaOlayemi
    files:
      - '**/*'
  cache:
    paths: []
